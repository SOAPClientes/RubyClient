<!DOCTYPE html>
<html>
<head>
  <title>Lista de Estudiantes Array</title>
</head>
<body>
  <center>
    <h1>Lista de Estudiantes Array</h1>
    <br><br>
    <% if @students.any? %>
      <div class="table-responsive">
        <table class="table table-striped table-bordered">
          <thead>
            <tr>
              <th>Nombre</th>
              <th>Apellido</th>
              <th>Nota</th>
              <th>Estado</th>
            </tr>
          </thead>
          <tbody>
            <% @students.each do |student| %>
              <tr class="<%= student[2].to_f >= 11 ? 'table-success' : 'table-danger' %>">
                <td><%= student[0] %></td>
                <td><%= student[1] %></td>
                <td><%= student[2] %></td>
                <td><%= student[2].to_f >= 11 ? 'Aprobado' : 'Desaprobado' %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <p>No se encontraron estudiantes.</p>
    <% end %>
    <br><br>
    <a href="/">Volver</a>
  </center>
</body>
</html>
