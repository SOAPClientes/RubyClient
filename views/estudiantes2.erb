<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lista de Estudiantes</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Arial', sans-serif;
    }
    h1 {
      margin: 20px 0;
      color: #343a40;
    }
    .container {
      margin-top: 50px;
    }
    .student-card {
      display: inline-block;
      text-align: center;
      width: 150px;
      margin: 10px;
    }
    .student-icon {
      font-size: 3rem;
    }
    .approved {
      color: green;
    }
    .disapproved {
      color: red;
    }
    .btn-custom {
      background-color: #007bff;
      color: white;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="text-center">
      <h1>Lista de Estudiantes</h1>
    </div>
    <% if @students.any? %>
      <div class="d-flex flex-wrap justify-content-center">
        <% @students.each do |student| %>
          <div class="student-card">
            <i class="fas fa-user student-icon <%= student[2].to_f >= 11 ? 'approved' : 'disapproved' %>"></i>
            <div><strong><%= student[0] %> <%= student[1] %></strong></div>
            <div>Nota: <%= student[2] %></div>
            <div>Estado: <%= student[2].to_f >= 11 ? 'Aprobado' : 'Desaprobado' %></div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="alert alert-warning text-center" role="alert">
        No se encontraron estudiantes.
      </div>
    <% end %>
    <div class="text-center">
      <a href="/" class="btn btn-custom mt-3">Volver</a>
    </div>
  </div>
  
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/js/all.min.js"></script>
</body>
</html>
